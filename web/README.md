# Web Frontend

This directory is reserved for the separated frontend application.

Planned frontend stack:

- React
- Vite
- TypeScript

Backend API and RAG evaluation logic remain in `rag_eval_pipeline/`.
